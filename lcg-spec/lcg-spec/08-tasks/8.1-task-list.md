# 08.1 — Task List

Tasks in order of execution. Check them off as you go. Each task should be small enough to complete in one Claude Code session.

## Phase 0 — Setup (30 min)

- [ ] Initialize Next.js 14 App Router project with TypeScript
- [ ] Install dependencies: tailwindcss, @supabase/ssr, @supabase/supabase-js, authorizenet, resend, react-email, lucide-react, date-fns, zod
- [ ] Configure Tailwind with warm color palette from `01-context/1.2-brand-and-design.md`
- [ ] Create folder structure from `02-architecture/2.1-overview.md`
- [ ] Set up `.env.example` with all required variables
- [ ] Initialize git, first commit

## Phase 1 — Database foundation (1-2 hours)

- [ ] Create Supabase project (production + staging)
- [ ] Write migration `0001_initial_schema.sql` with all tables from `03-data-model/3.1-schema.md`
- [ ] Write migration `0002_rls_policies.sql` with all policies
- [ ] Write migration `0003_seed_settings.sql` with default settings rows
- [ ] Write migration `0004_seed_vendors.sql` with known vendors (Houston's, Sunny Sky, Mocafe, David Rio, Monin, Torani, Kerry, DaVinci Gourmet, iBev, Smartfruit, Tiki Breeze)
- [ ] Run migrations, confirm schema in Supabase dashboard
- [ ] Generate TypeScript types via Supabase CLI (`supabase gen types typescript > src/types/db.ts`)
- [ ] Create `lib/supabase/{server,browser,admin}.ts` clients

## Phase 2 — Public site shell (2-3 hours)

- [ ] Root layout with global CSS vars, fonts, metadata defaults
- [ ] Shop layout (`(shop)/layout.tsx`) with header + footer
- [ ] Header component: logo, nav (Teas, Cocoa, Frappes, Oatmeal, Smoothies, Syrups, Protein, Brands), search, cart icon, account icon, phone number prominent
- [ ] Footer component: address, hours, contact, newsletter signup, legal links, trust badges
- [ ] Homepage with: hero, featured products, category grid, "since 2003" section, testimonials area (placeholder), newsletter signup
- [ ] Static pages: About, For Business, Contact, Shipping Policy, Privacy, Terms
- [ ] 404 page and 500 page with personality

## Phase 3 — Catalog pages (2-3 hours)

- [ ] Category page `(shop)/[category]/page.tsx` — grid of products, filters (brand, price range), sort
- [ ] Brand page `(shop)/brand/[slug]/page.tsx`
- [ ] Product detail page `(shop)/[productSlug]/page.tsx` (flat URLs for SEO — see migration doc)
  - Image gallery
  - Title, brand, short description
  - Price, pack size
  - Add-to-cart quantity selector
  - Full description tabs
  - "Customers also bought" section (same-brand products)
- [ ] Search results page
- [ ] Sitemap route `/sitemap.xml` generated dynamically
- [ ] robots.txt
- [ ] Schema.org JSON-LD on product + org + breadcrumb

## Phase 4 — Cart + Checkout (3-4 hours)

- [ ] Cart page with update/remove, subtotal, estimated shipping, proceed button
- [ ] Cart persistence: logged-in uses DB `carts` table, guest uses cookie + sessionStorage fallback
- [ ] Checkout page (single-page, collapsible sections)
- [ ] Accept.js integration for card entry
- [ ] `/api/checkout/tax` — returns $0 by default, ready for phase 2 TaxJar swap-in
- [ ] `/api/checkout/create` — create order, submit auth_capture to Auth.net, handle all 3 outcomes
- [ ] Payment state machine handling: approved, held, declined
- [ ] `/checkout/confirmation` page
- [ ] `/api/webhooks/authnet` — signature verify, update payment/order status
- [ ] Order confirmation email via Resend

## Phase 5 — Customer accounts (2-3 hours)

- [ ] Auth pages: `/login` (with tabs for magic-link AND email+password), `/register`, `/forgot-password`, `/reset-password`
- [ ] Supabase Auth configured: enable email+password AND magic link methods
- [ ] Magic link email template (branded, not Supabase default)
- [ ] `/account` dashboard with quick reorder + recommendations
- [ ] `/account/orders` list + detail
- [ ] `/account/addresses` CRUD
- [ ] `/account/payment-methods` with Auth.net CIM integration
- [ ] `/account/profile` with business info toggle + "set password" option for magic-link-only users
- [ ] Reorder flow: "Add all to cart" from past order

## Phase 6 — Admin core (3-4 hours)

- [ ] Admin shell layout with sidebar, auth guard
- [ ] `/admin` dashboard with today's numbers
- [ ] `/admin/orders` list + detail
- [ ] Per-vendor email composer component with warehouse dropdown, preview modal, send button
- [ ] `/api/admin/orders/[id]/send-vendor-email` — uses Gmail API to send from grandpa's account
- [ ] Tracking paste-back form, triggers customer shipping notification email
- [ ] `/admin/products` list + detail + edit
- [ ] `/admin/vendors` list + detail
- [ ] `/admin/customers` list + detail
- [ ] `/admin/settings` with tabs

## Phase 7 — Admin finance (2-3 hours)

- [ ] `/admin/finance/sales` with date picker, charts, tables
- [ ] `/admin/finance/net-revenue` with estimated fee calc
- [ ] `/admin/finance/margin` with per-product breakdown
- [ ] `/admin/finance/disputes` tracking view
- [ ] `/admin/finance/quickbooks` with connect button, mapping, export
- [ ] QBO OAuth callback handler
- [ ] Daily QBO sync cron (stub; wire up after grandpa configures mapping)

## Phase 8 — Product import tooling (1-2 hours)

- [ ] `/admin/products/import` upload page
- [ ] Parser for CSV / XLSX (use `xlsx` npm package)
- [ ] Column mapping UI with saved mappings per vendor
- [ ] Preview table + dry run
- [ ] Commit import with audit log

## Phase 9 — Migration (2-3 hours, once BC access and vendor sheets arrive)

- [ ] Script: download BC product export, parse, stage in temp table
- [ ] Script: download BC images, upload to Supabase Storage
- [ ] Script: import BC customers with temp passwords + reset flags
- [ ] Script: import BC order history
- [ ] Script: generate redirect map from BC URLs
- [ ] Wire up middleware.ts with redirect map
- [ ] Import vendor catalog(s) via the import UI

## Phase 10 — Pre-launch polish (2-3 hours)

- [ ] Run through every item in `07-launch/7.1-launch-plan.md` checklist
- [ ] Lighthouse audit on homepage, category, product — fix anything scoring < 85
- [ ] Mobile responsive check on all critical pages
- [ ] Dark mode? — NO, skip v1
- [ ] Error boundaries on admin and shop routes
- [ ] Analytics: add Google Analytics 4 or Plausible (grandpa picks)
- [ ] Grandpa UAT session: screen share, walk him through everything, take notes

## Phase 11 — Deploy staging (1 hour)

- [ ] SSH to Orange VPS, provision `/var/www/lcg-staging`
- [ ] PM2 ecosystem config
- [ ] Nginx subdomain config
- [ ] Let's Encrypt SSL for staging subdomain
- [ ] Deploy + smoke test

## Phase 12 — Launch day

- [ ] Reduce DNS TTL 24h ahead
- [ ] Cut DNS over
- [ ] Send password-reset email blast
- [ ] Submit new sitemap to Google
- [ ] Monitor for 72 hours

---

## Progress tracking

Use `08-tasks/progress.md` — update after each session with what was completed.
