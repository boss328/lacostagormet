# 08.2 — Progress

Update this after every Claude Code session. Each session should start by reading this to know where we are.

## Current status
**Last updated:** 2026-04-18 (Phase 3C steps 3-5 complete — PDP, cart, For Business)
**Current phase:** Phase 3 — public catalog UI with real BC imagery
**Next task:** Phase 4 — checkout flow (Accept.js), order creation, shipping + tax wiring

## Completed

- Full spec written across 17 files
- BigCommerce exports received and saved to `references/bigcommerce/`:
  - products-export.csv (158 products, 122 visible)
  - orders-export.csv (3,139 valid orders, $571K lifetime revenue)
  - shipments-export.csv (tracking history)
  - customers-export.xml (6,337 customers, 12,025 addresses)
- Business analysis completed — see `references/bigcommerce/README.md` for findings
- **Phase 0 — Setup (done 2026-04-18):** Next.js 14 + TS strict + Tailwind (warm palette via CSS vars) + Fraunces/JetBrains_Mono via next/font + Supabase SSR clients (server/browser/admin) + folder tree per 02.1 + dev server on port 3001
- **Phase 1 — Database (done 2026-04-18):** 3 migrations applied in Supabase — 22 tables, 6 enums, `order_number_seq`, 15 indexes, full RLS (30+ policies), seeded 12 vendors (incl. Home/Garage), 12 brands, 6 top-level categories, 7 settings rows
- **Phase 2 — Product catalog migration (done 2026-04-18):** 121 / 122 BC products imported via `scripts/migrate-products.ts` (one SKU-less row skipped — see TODOs). 361 product_images, 114 product_categories links. 2 new brands created on-the-fly (Mylk Labs, Sunny Sky Products).
- **Phase 3C — Real image URLs (done 2026-04-18):** Migration rewritten for v3 BC export (`products-export-v3.csv`), which interleaves `Item=Product` and `Item=Image` rows with the real CDN URL in the `Internal Image URL (Export)` column. Re-ran against 121 existing SKUs — UPSERT preserved brand/category/vendor FKs (narrow payload: no brand/category/vendor columns in upsert). 361 images replaced with full URLs. Canonical size stored = 1280.1280 (largest BC CDN reliably serves; verified 350/500/1280 are the only universal variants — 1000/2048 404). Render-time `src/lib/bcImage.ts` helper downsizes to card (350) / mid (500) / hero (1280). `next.config.mjs` remotePatterns fixed (`/products/**` not `/product_images/**`). `NEXT_PUBLIC_BC_CDN_BASE` env var and runtime 404 fallback logic removed — URLs are now complete and verified.
- **Phase 3C step 3 — Product detail page (done 2026-04-18):** `/product/[slug]` server component at `src/app/(shop)/product/[slug]/page.tsx`. Breadcrumb → 2-column hero (60/40 gallery/info) → description band (paper-2, rich text + key-value aside) → related products Reveal section. `ProductGallery` client component: main image + thumbnail strip, click-to-swap, horizontal scroll for 7+ images, branded fallback for zero-image SKUs. `ProductAddPanel` client: quantity stepper (minus/plus, 1-99, lucide icons) + full-width solid Add-to-cart button, "Added" microstate. `ProductDescription`: sanitises BC rich text — strips `<style>`, `<script>`, `<font>`, and inline `style/class/id/color/bgcolor/face/size/align` attributes — preserves list/heading/strong/em semantics. Italic accent heuristic: walks tokens right-to-left, skipping pack-size suffix words (Bags, lb, Five, 3.5) and numerics, to land on the last substantive noun (e.g. "Mix" in "Big Train Vanilla Latte … Mix Five 3.5 lb. Bags"). Related = 4 other SKUs from same brand; falls back to same primary category if brand has <3 alternates.
- **Phase 3C step 4 — Cart + Zustand store (done 2026-04-18):** `src/stores/cart.ts` — Zustand 5 + `persist` middleware, localStorage key `lcg-cart-v1`, version 1. Actions: `addItem`, `removeItem`, `updateQuantity`, `clear`. Selector helpers: `selectSubtotal`, `selectItemCount`, free-shipping / tier-1 / tier-2 threshold predicates with exported constants ($70 / $400 / $700). `/cart` page at `src/app/(shop)/cart/page.tsx` — header renders at SSR, content area 70/30 two-column after hydration. Line items: 80px thumbnail, brand/name/pack, stepper, remove link, italic line-total price. Sticky summary: subtotal, shipping (italic gold FREE / $12.99 with "add $X for free" hint), volume tier badges (gold = tier 1, brand-deep = tier 2), italic serif pricing-inquiry note, italic total with floating cents, disabled Checkout button ("Checkout coming soon — Phase 4"). Empty state: centered paper-2 card with "Ready when you are." italic + Shop the Catalog CTA. `CartBadge` extracted — Nav now uses a client component that subscribes to `selectItemCount` with SSR-safe hydration guard. `ProductCardAdd` client button replaces the static Add pill on `ProductCard` — `stopPropagation()` prevents the surrounding `<Link>` from navigating when the user wants to add-in-place. Handles all edge cases: image-less products (SKU fallback), single vs. plural pluralisation of "item"/"line", qty 0 auto-removes via `updateQuantity`, NULL pack-size omits the pack line.
- **Phase 3C step 5 — For Business page (done 2026-04-18):** `/for-business` at `src/app/(shop)/for-business/page.tsx`. Hero: dark `brand-darker` bg with gold radial + diagonal texture, 2-col split. Left: mono eyebrow "For cafés, offices & independent business" → "A trusted *partner* since 2003." h1 (76px custom, italic accent in `type-accent-gold`) → lede grounded in real numbers (22 years / 121 SKUs / 14 brands / 6-week reorder rhythm) → `outline-gold` anchor buttons. Right: glass inset card with "The short version", placeholder quote, and 4-row dark-stat dl. Volume tiers: paper-2 band, SectionHead § I, 3-col grid with `I / II / III` italic roman numerals, price-style threshold, bullet benefits, footnote linking to #contact. Testimonials: paper band, SectionHead § II, 3-up figures with large italic `"` glyph, blockquote serif italic 22px, small-caps mono attribution (NAME · ROLE · PLACE); 3 placeholder quotes flagged with `<!-- TODO: replace with real testimonials from Jeff's customers -->`. Contact section: paper-2 band, SectionHead § III, 2-col. Left = non-interactive form shell (name, business, email, phone, volume estimate select, notes textarea, disabled submit + Phase 5 wiring note). Right = direct-contact card with italic brand-deep phone placeholder `(760) XXX-XXXX` (TODO for Jeff's real number), email mailto, warehouse, est. year. `Reveal` component extended with optional `id` prop so the `#contact` anchor resolves correctly when the section is wrapped for scroll-reveal.

## Blocked / waiting on

- Google Workspace OAuth credentials / app password (grandpa to send)
- BigCommerce admin login access (for any missed exports — may or may not need this now)
- Vendor spreadsheets (grandpa to send — Houston's, Dr. Smoothie, Mocafe, others)
- Authorize.net AFDS rules export (grandpa to send)
- Original logo file high-res (grandpa to send)
- Reference sites grandpa likes (grandpa to send)
- QuickBooks Online Chart of Accounts mapping (conversation with grandpa)

## Decisions made mid-build

- **Design system locked at v5 "cinematic" level (2026-04-18).** Authoritative spec is `01-context/1.3-design-system.md`. Key choices: Fraunces serif (display) + JetBrains Mono (labels) via next/font/google — NO Inter, Georgia, or system fonts. Warm mercantile palette with new hex values (ink #1A110A, paper #F6EEDE, brand-deep #4E2410, gold #B88A48, accent #C14828). Italic accent words in every headline (e.g., "Shop by *category*"). Catalog-style section numbering (§ I, § II, § III) with roman numerals. Staggered page-load animations (0.1s, 0.25s, 0.4s, 0.55s delays) and IntersectionObserver-based scroll reveals. Real photography with uniform warm color grading. Specific hover transitions defined per component (product cards: lift + dual shadow + image zoom; brand rows: accent bar draw + slide right; buttons: overlay-slide fill). Every page uses a named template (CatalogPage, ProductDetailPage, CartPage, CheckoutPage, AccountPage, AdminPage). Admin uses same design language, just denser. Phase 3 prompt rewritten in the Claude Code playbook to enforce all of this.
- **Final fulfillment model (post-Jeff-call, 2026-04-17):** one unified catalog, one `supplier` dropdown per product. Vendors table seeds: Houston's, Sunny Sky, Mocafe, David Rio, Monin, Torani, Kerry, DaVinci Gourmet, iBev, Smartfruit, Tiki Breeze, and a "Home/Garage" row with `is_self_fulfilled=true`. No per-warehouse routing. No label printing integration. Orders group by supplier → admin generates pre-filled email Jeff can edit → sends via Gmail API. Home/Garage items land in `/admin/ship-yourself` queue with no supplier email; Jeff prints labels via his existing USPS/UPS/PayPal workflow.
- **Jeff writes his own email templates.** Seeded with a basic placeholder template; will be replaced when Jeff sends real copy. Template variables: `{{order_number}}`, `{{customer_name}}`, `{{shipping_address}}`, `{{items_list}}`, `{{po_number}}`, `{{my_account_number}}`.
- **Tracking API deferred to Phase 2.** Jeff said "push back and forth if easy, Phase 2 if complex." We need to see real supplier reply emails before we can scope it. V1 = manual paste-back. Revisit 30-60 days post-launch once we have supplier reply data.
- **Zero sales tax in v1 — TaxJar is v2-optional.** Jeff's entire 158-SKU catalog is non-taxable grocery food. CA (his only nexus state) doesn't tax groceries. Build uses a pluggable `calculateTax()` function that returns `0` by default; swapping in TaxJar later is a 2-hour task if needed. Agreement has a clause making client responsible for notifying Builder if taxable products are added or nexus is crossed in a new state.
- **Amazon Storefront IS real and IS out of v1 scope.** Jeff runs an Amazon Seller Central storefront in addition to the website. Those orders currently ship via Houston's email handoff same as the website. Jeff wants Amazon orders eventually visible in the new admin, but Amazon SP-API is a multi-week integration (plus 1-3 weeks of Amazon's approval process) that would blow the weekend timeline and the $1,500 price. **V1 is website-only; Amazon stays in Seller Central as it does today.** Schema includes `orders.source` column and `order_source` enum so Phase 2 integration slots in cleanly. Phase 2 priced separately at $2,500-4,000 when the time comes.
- **Big Train concentration risk flagged.** Big Train + likely-Big-Train-miscategorized products account for the vast majority of revenue. The new admin should surface vendor diversification (what % of revenue comes from which vendor) as a metric Jeff can watch.
- **Catalog migration strategy:** prefer vendor spreadsheets as source of truth; use BC export only for product slugs (URL preservation), Jeff's custom descriptions, and product images.
- **Volume pricing display:** "Volume discounts on orders $400–$700+" copy on For Business page. Actual tier mechanics (auto-applied % vs. "contact for quote") TBD pending conversation with Jeff.
- **Cart persistence decision (2026-04-18):** Zustand store persisted to localStorage (key `lcg-cart-v1`) rather than server-side session. Rationale: v1 supports both guest and authenticated shopping, and a localStorage cart survives across auth state changes without requiring a server round-trip on every add. Server-side cart reconciliation (guest → authed merge) happens at checkout-start in Phase 4. `persist` middleware writes on every store mutation; reads hydrate async after mount, so every cart-aware surface (cart page, CartBadge) uses a `hydrated` boolean guard to avoid React SSR hydration mismatches.
- **Cart count in Nav (2026-04-18):** Nav stayed a server component; only the cart badge was extracted to `CartBadge` (client). This minimises the client-component boundary — the hamburger, search icon, account link, and wordmark all stay server-rendered.
- **ProductCard Add-to-cart click (2026-04-18):** The card is a `<Link>` wrapping the whole tile. Rather than restructure (a common DTC pattern is to have Add inside the link and trigger cart on navigate), we kept the whole tile clickable AND made Add an in-place action by giving `ProductCardAdd` a `stopPropagation + preventDefault` handler. Clicks on the image/name/price/Add-pill all navigate to the PDP; clicks on Add stay and add to cart. Matches the editorial catalog feel (the tile leads with the product, not the purchase action).
- **PDP italic accent picker (2026-04-18):** Walks tokens right-to-left and skips pack-size units (lb/oz/kg/g), vessels (Bags/Cans/Bottles/...), counting words (Five/Twelve), bare numerics, and punctuation-only tokens. First survivor gets italic `type-accent`. Handles real product names like "Big Train Vanilla Latte Blended Ice Coffee Beverage Mix Five 3.5 lb. Bags" → accent lands on "Mix".

## Issues encountered

- ~~**BC image paths in the CSV aren't current CDN URLs (2026-04-18).**~~ **Resolved 2026-04-18 (Phase 3C):** Jeff re-exported as v3 which includes the full CDN URL in `Internal Image URL (Export)`. Migration rewritten against the v3 row structure. `ImageWithFallback` kept but simplified — only the null-src branch remains for products without images (currently zero such products in catalog).

## TODOs discovered mid-build

- Brand metadata cleanup during migration: "Unknown" brand on 74% of SKUs in orders; match by SKU prefix (BT. = Big Train, etc.) during import
- Ship method normalization: current data has "USPS", "usps", "USpS", "UPS/FedEx" — need controlled vocabulary
- 68 catalog SKUs with zero historical sales — candidates for archiving on launch rather than importing
- One product skipped during migration: "Mylk Labs Oatmeal Roasted Almond 6 Cups" has no SKU in BC. Either Jeff adds a SKU and re-exports, or he creates the product fresh in admin UI post-launch.
- 10 products imported with `primary_category_id = NULL` (Specialty Products / niche items that didn't match a top-level category alias). Jeff categorizes these via `/admin/products`.
- 2 brands created on-the-fly during product migration that weren't in the original 12-brand seed: Mylk Labs, Sunny Sky Products. Review whether Sunny Sky Products should merge into a seeded brand or stay distinct.
- **(step 3)** PDP fetches `weight_lb` and renders it in the Details aside when present; older BC imports may have NULL. Weight line is omitted silently when NULL (same as pack_size behaviour).
- **(step 4)** Checkout CTA on `/cart` is visually disabled with a "Phase 4" caption. Once checkout lands, swap the button to `<Button variant="solid" arrow href="/checkout">`.
- **(step 4)** Cart badge in Nav always shows "0" on SSR, then hydrates to the real count. The flash is a handful of ms — acceptable, but noted in case we want to move to cookie-based SSR count later.
- **(step 5)** For Business form is a non-interactive shell. `action=""` `method="post"` prevents the `<button disabled>` from being a true submit — pressing Enter in a text input is blocked by the disabled default submit button. When Phase 5 lands, wire to a server action or `/api/business-inquiries` + Resend notification to Jeff.
- **(step 5)** Phone number placeholder `(760) XXX-XXXX` and three testimonial quotes are flagged with inline HTML comments / TODO tags for Jeff to replace during review.
